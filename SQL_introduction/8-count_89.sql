-- Counts the number of records in 'first_table' where the 'id' column is equal to 89.
-- This command is useful for checking if a specific record with 'id' = 89 exists
-- and can help in verifying successful inserts or checking for duplicates.

SELECT COUNT(*) FROM first_table WHERE id = 89;
