-- Retrieves all records from the 'first_table' table.
-- The '*' wildcard selects all columns in the table, returning every row and all column data.
-- This command is useful for viewing the entire contents of 'first_table' for review or analysis.

SELECT * FROM first_table;
