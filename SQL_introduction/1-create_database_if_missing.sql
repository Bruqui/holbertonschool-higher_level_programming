-- Creates a new database named 'hbtn_0c_0' if it doesn't already exist.
-- The 'IF NOT EXISTS' clause prevents errors if the database is already present,
-- ensuring the command only creates the database if it is missing.
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
