-- Drops (deletes) the database named 'hbtn_0c_0' if it exists.
-- The 'IF EXISTS' clause prevents an error if the database does not exist,
-- ensuring that the command runs smoothly whether or not the database is present.

DROP DATABASE IF EXISTS hbtn_0c_0;
